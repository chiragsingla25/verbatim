# AGENTS.md

This project's working context lives in [`CLAUDE.md`](CLAUDE.md). Tools that don't read `CLAUDE.md`
should read that file — it is the source of truth for architecture, stack, build phases, commands,
and conventions. Keep the two in sync if you edit either.
