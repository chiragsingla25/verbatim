# Copy to .env.local. These are the only local vars — both are public (RLS-gated).
# Server secrets (LLM, service-role, webhook, GitHub PAT) are set via `supabase secrets`
# and GitHub Actions secrets — see CLAUDE.md.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_BASE=/verbatim/
